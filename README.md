# ai-test

Sandbox repository used as a research-only git submodule under sibling Flutter SDK projects (e.g. `references/ai-test/` in `uptizm-app`).

Empty by design — exists so submodule add works against a non-empty target. Push experimental notes, prototype scripts, or AI-related research artifacts here.
