name: ai_test_flutter
description: Flutter Web LLM-agent test plugin (V3 — MCP-only single-channel via VM Service custom extensions).
version: 0.0.1-dev
publish_to: none

environment:
  sdk: "^3.6.0"
  flutter: ">=3.27.0"

executables:
  ai_test_flutter: ai_test_flutter

dependencies:
  flutter:
    sdk: flutter
  web: ^1.0.0
  meta: ^1.10.0
  magic: ^1.0.0-alpha.12
  logging: ^1.2.0
  image: ^4.0.0
  args: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
