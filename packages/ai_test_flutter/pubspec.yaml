name: ai_test_flutter
description: Flutter Web E2E testing via Shadow DOM Projection. Spike package under active validation.
version: 0.0.1-dev
publish_to: none

environment:
  sdk: "^3.6.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.0.0
  meta: ^1.10.0
  magic: ^1.0.0-alpha.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
