name: ai_test_flutter_example
description: V3 fixture app — exercises every plugin extension via integration_test.
version: 0.0.1-dev
publish_to: none

environment:
  sdk: "^3.6.0"
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  ai_test_flutter:
    path: ../
  http: ^1.2.0
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  magic: ^1.0.0-alpha.12
